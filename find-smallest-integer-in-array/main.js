class SmallestIntegerFinder {
  findSmallestInt(args) {
    // Pass in each item in the array to Math.min with
    // the spread operator
    return Math.min(...args);
  }
}

//Given an array of integers your solution should find the smallest integer.
